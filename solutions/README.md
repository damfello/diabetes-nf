# Solutions

Reference solution for the Diabetes Challenge. Open it alongside the source notebook to check your work.

| File / Folder | Description |
|---|---|
| [**1 - Diabetes Challenge**](1_diabetes_challenge.ipynb) | Worked solution: pull and join the data from the database, clean it, handle missing values and class imbalance, then build, tune, and evaluate classifiers. |

> [!NOTE]
> The solution loads its data from the database, so it needs a populated `.env` (see the repository Setup) to run.
